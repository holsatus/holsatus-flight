[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --always-print-stacktrace --chip RP2040"
linker = "flip-link"

[build]
target = "thumbv6m-none-eabi"

[env]
DEFMT_LOG = "debug"

[unstable]
build-std = ["compiler_builtins", "core"]